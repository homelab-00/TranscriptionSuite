# CAVA configuration file for Python wrapper PoC

[input]
method = pulse

source = "alsa:acp:Generic:0:capture"

[general]
# This sets the number of bars. 0 means it will fill the terminal width automatically.
bars = 0

[output]
# This sets the output channel format.
channels = mono
